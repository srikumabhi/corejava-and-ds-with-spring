package com.corejava.practice.javapractices.basicsofjava.java9.httpby2;

import java.io.IOException;
import java.net.URI;

import org.springframework.stereotype.Service;

//import jdk.incubator.http.HttpClient;
//import jdk.incubator.http.HttpRequest;
//import jdk.incubator.http.HttpResponse;

/**
 * 
 * @author Administrator
 *
 */
@Service("httpby2")
public class HttpBy2 {

	/**
	 * 
	 */
	/*
	 * public void HttpBy2Impl(){ try { HttpClient httpClient =
	 * HttpClient.newHttpClient();
	 * 
	 * HttpRequest httpRequest =
	 * HttpRequest.newBuilder(URI.create("http://www.google.com"))
	 * .header("User-Agent","Java") .GET() .build();
	 * 
	 * HttpResponse<String> httpResponse = httpClient.send(httpRequest,
	 * HttpResponse.BodyHandler.asString());
	 * 
	 * System.out.println("httpResponse=> "+httpResponse); }catch (IOException |
	 * InterruptedException e) { e.printStackTrace(); }
	 * 
	 * }
	 */

}
